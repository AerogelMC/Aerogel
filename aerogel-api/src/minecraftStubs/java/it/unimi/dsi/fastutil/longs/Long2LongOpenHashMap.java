package it.unimi.dsi.fastutil.longs;

/** Compile-time name stub. Not included in the Aerogel API JAR. */
public class Long2LongOpenHashMap implements Long2LongMap {
    @Override public long get(long key) { return 0L; }
    @Override public long put(long key, long value) { return 0L; }
    @Override public long remove(long key) { return 0L; }
    @Override public boolean containsKey(long key) { return false; }
}
