package net.minecraft;

import net.minecraft.world.level.LevelHeightAccessor;

/** Compile-time name stub. Not included in the Aerogel API JAR. */
public class CrashReportCategory {
    public static String formatLocation(LevelHeightAccessor level, int x, int y, int z) { return null; }
    public CrashReportCategory setDetail(String name, CrashReportDetail<String> detail) { return this; }
}
